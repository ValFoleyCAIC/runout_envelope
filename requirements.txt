# Core scientific / geospatial
numpy>=1.24
pandas>=2.0
scipy>=1.10
pyyaml>=6.0

# Geospatial I/O
rasterio>=1.3
fiona>=1.9
shapely>=2.0
geopandas>=0.14
pyproj>=3.6

# Plotting / reports
matplotlib>=3.7
jinja2>=3.1
folium>=0.15                     # Leaflet map generator
branca>=0.7                      # Folium helper for colormaps
scikit-image>=0.22               # skeletonize for medial-axis path

# Avalanche physics (install separately per AvaFrame docs)
# avaframe>=1.12

# Entrainment auto-detection via flow accumulation
# richdem>=0.3   # uncomment if using richdem for flow routing
